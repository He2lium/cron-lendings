import { Rubik } from 'next/font/google';

export const rubikSans = Rubik({
  variable: '--font-geist-sans',
  subsets: ['latin'],
  weight: '400',
});

export const rubikSansLight = Rubik({
  variable: '--font-geist-sans',
  subsets: ['latin'],
  weight: '300',
});
